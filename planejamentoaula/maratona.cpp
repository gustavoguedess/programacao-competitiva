#include<stdio.h>

int main(){
    int w, h;
    int n;
    int x1,y1, x2,y2;
    pair<int,int> h[MAX],v[MAX];

    cin >> w >> h;
    cin >> n;
    for(int i=0; i<n; i++){
        cin >> x1 << y1 << x2 << y2;
        if(x1)
    }
    return 0;
}